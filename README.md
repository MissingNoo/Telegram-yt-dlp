Needs to have https://github.com/tdlib/telegram-bot-api running with --local

1. Run prerun.sh to create the python venv

2. Put your bot token into the bot.env file

3. run start.sh to start the bot
